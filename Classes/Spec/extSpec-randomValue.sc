+ Spec{
    randomValue{
        var min = this.minval;
        var max = this.maxval;
        ^rrand(min, max);
    }
}

+ ArrayedSpec {
    randomValue{
        var min = this.minval;
        var max = this.maxval;
        ^rrand(min, max);
    }
}
